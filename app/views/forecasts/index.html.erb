<p id="notice"><%= notice %></p>

<h1>Forecasts</h1>

<table>
  <thead>
    <tr>
      <th>Temp</th>
      <th>Lat</th>
      <th>Long</th>
      <th>Snow</th>
      <th>Precip</th>
      <th>Timestamp utc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @forecasts.each do |forecast| %>
      <tr>
        <td><%= forecast.temp %></td>
        <td><%= forecast.lat %></td>
        <td><%= forecast.long %></td>
        <td><%= forecast.snow %></td>
        <td><%= forecast.precip %></td>
        <td><%= forecast.timestamp_utc %></td>
        <td><%= link_to 'Show', forecast %></td>
        <td><%= link_to 'Edit', edit_forecast_path(forecast) %></td>
        <td><%= link_to 'Destroy', forecast, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Forecast', new_forecast_path %>
